function [ output_args ] = processObject( userx, usery, objectx, objecty, widthx, widthy, offsetx, offsety )
    if ~isvector(userx) || ~isvector(usery)
        error('Input must be a vector')
    end
 
    
end

